name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

    steps:
    - uses: actions/checkout@v3
    - name: Setup
      run: |
        rustup toolchain add nightly --component clippy
        rustup override set nightly
    - name: Build runtime
      run: cargo build --verbose -p continuate-rt
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose --workspace
    - name: Run Clippy
      run: cargo clippy --verbose --workspace
  miri:
    name: Test with Miri
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup
        run: |
          rustup toolchain add nightly --component miri
          rustup override set nightly
          cargo miri setup
      - name: Run Miri
        run: cargo miri test --verbose --workspace
